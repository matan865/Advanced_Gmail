<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/tvHello"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Choose mailbox"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp"/>

    <Button
        android:id="@+id/btnInbox"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Inbox"
        app:layout_constraintTop_toBottomOf="@id/tvHello"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginTop="24dp"/>

    <Button
        android:id="@+id/btnSent"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Sent"
        app:layout_constraintTop_toBottomOf="@id/btnInbox"
        app:layout_constraintStart_toStartOf="@id/btnInbox"
        app:layout_constraintEnd_toEndOf="@id/btnInbox"
        android:layout_marginTop="12dp"/>

    <Button
        android:id="@+id/btnDrafts"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Drafts"
        app:layout_constraintTop_toBottomOf="@id/btnSent"
        app:layout_constraintStart_toStartOf="@id/btnInbox"
        app:layout_constraintEnd_toEndOf="@id/btnInbox"
        android:layout_marginTop="12dp"/>

    <Button
        android:id="@+id/btnSpam"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Spam"
        app:layout_constraintTop_toBottomOf="@id/btnDrafts"
        app:layout_constraintStart_toStartOf="@id/btnInbox"
        app:layout_constraintEnd_toEndOf="@id/btnInbox"
        android:layout_marginTop="12dp"/>

    <Button
        android:id="@+id/btnTrash"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Trash"
        app:layout_constraintTop_toBottomOf="@id/btnSpam"
        app:layout_constraintStart_toStartOf="@id/btnInbox"
        app:layout_constraintEnd_toEndOf="@id/btnInbox"
        android:layout_marginTop="12dp"/>


</androidx.constraintlayout.widget.ConstraintLayout>
